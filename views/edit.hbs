{{>nav}}

<form action="/todos/{{todo.id}}?_method=PUT" method="POST">
  <input class="form-check-input" type="checkbox" value="completed" name="isCompleted" {{#if todo.isCompleted}}checked{{/if}}>
  <input type="text" placeholder="Please input todo name" name="name" value="{{todo.name}}" required>
  <button type="submit">Save</button>
</form>

{{>message}}