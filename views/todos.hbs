{{>nav}}

<a href="/todos/new">Create</a>
<ul>
  {{#each todos}}
  <li style="margin-top:5px;">
    <input class="form-check-input" type="checkbox" id="flexCheckDefault" name="isCompleted" {{#if this.isCompleted}}checked{{/if}} disabled>
    {{this.name}}
    <a href="/todos/{{this.id}}">Detail</a>
    <a href="/todos/{{this.id}}/edit">Edit</a>
    <form action="/todos/{{this.id}}?_method=DELETE" method="POST" style="display: inline;">
      <button type="">Delete</button>
    </form>
  </li>
  {{/each}}
</ul>
<form action="/todos" method="GET" style="display: inline;">
  <input type="hidden" name="page" value="{{prev}}">
  <button type="submit">prev</button>
</form>
<form action="/todos" method="GET" style="display: inline;">
  <input type="hidden" name="page" value="{{next}}">
  <button type="submit">next</button>
</form>
<span>page: {{page}}</span>

{{>message}}